# Dependencies / build output (rebuilt inside the image)
node_modules
client/node_modules
client/dist

# Local database — never bake it into the image
data

# Secrets & local config
.env
.env.*

# VPS-only tooling not used on Fly
ecosystem.config.js
tunnel.js
deploy.ps1
deploy.sh

# Misc
.git
.gitignore
*.log
npm-debug.log*
.DS_Store
